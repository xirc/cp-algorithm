#include <iostream>
#include "../template-main.hpp"
#include "./vector3.hpp"

using namespace std;

void setup(string& header, map<string,Command>& commands) {
    header = "vector3";
    // Do nothing, Just check compilable.
}