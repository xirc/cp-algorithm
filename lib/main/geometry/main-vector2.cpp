#include <iostream>
#include "template/template-main.hpp"
#include "cpalgo/geometry/vector2.hpp"

using namespace std;

void setup(string& header, map<string,Command>& commands) {
    header = "vector2";
    // Do nothing, Just check compilable.
}