#include <bits/stdc++.h>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(0); cout.tie(0);

    string S;
    for (int i = 0; i < 3; ++i) {
        cin >> S;
        cout << S[i];
    }
    cout << endl;

    return 0;
}