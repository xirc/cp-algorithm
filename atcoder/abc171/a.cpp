#include <bits/stdc++.h>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(0); cout.tie(0);

    char c;
    cin >> c;
    if (islower(c)) {
        cout << "a" << endl;
    } else {
        cout << "A" << endl;
    }

    return 0;
}