#include <bits/stdc++.h>

using namespace std;
using ll = int64_t;
using ff = long double;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(0); cout.tie(0);

    int A, P;
    cin >> A >> P;
    int num_pies = (A * 3 + P) / 2;
    cout << num_pies << endl;

    return 0;
}