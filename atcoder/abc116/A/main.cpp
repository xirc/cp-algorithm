#include <bits/stdc++.h>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(0); cout.tie(0);
    
    int AB, BC, CA;
    cin >> AB >> BC >> CA;
    cout << (AB * BC) / 2 << endl;

    return 0;
}